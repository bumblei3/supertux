diff --git a/CMakeLists.txt b/CMakeLists.txt
index 2938945..997a736 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,53 +1,53 @@
 cmake_minimum_required(VERSION 3.0...4.0)
-project(SDL2_TTF C)
+project(SDL3_TTF C)
 
 set(CMAKE_MODULE_PATH "${CMAKE_MODULE_PATH} ${CMAKE_CURRENT_SOURCE_DIR}/mk/cmake")
 
-option(SDL2TTF_INSTALL "Install target for SDL2_ttf." OFF)
-option(SDL2TTF_RAQM "Build with RAQM" OFF)
+option(SDL3TTF_INSTALL "Install target for SDL3_ttf." OFF)
+option(SDL3TTF_RAQM "Build with RAQM" OFF)
 
-add_library(SDL2_ttf SDL_ttf.c version.rc)
+add_library(SDL3_ttf SDL_ttf.c version.rc)
 
-set_target_properties(SDL2_ttf PROPERTIES DEFINE_SYMBOL SDL2_EXPORTS)
-target_include_directories(SDL2_ttf PUBLIC ${PROJECT_SOURCE_DIR})
+set_target_properties(SDL3_ttf PROPERTIES DEFINE_SYMBOL SDL3_EXPORTS)
+target_include_directories(SDL3_ttf PUBLIC ${PROJECT_SOURCE_DIR})
 
 if(TARGET RAQM)
   message(STATUS "Compiling SDL_ttf with RAQM")
-  set(SDL2TTF_RAQM ON)
+  set(SDL3TTF_RAQM ON)
 else()
   message(STATUS "SDL_ttf will not be compiled with RAQM")
 endif()
 
-if(SDL2TTF_RAQM AND (TARGET RAQM))
-  target_compile_definitions(SDL2_ttf PRIVATE HAVE_RAQM=1)
-  target_link_libraries(SDL2_ttf PRIVATE RAQM)
+if(SDL3TTF_RAQM AND (TARGET RAQM))
+  target_compile_definitions(SDL3_ttf PRIVATE HAVE_RAQM=1)
+  target_link_libraries(SDL3_ttf PRIVATE RAQM)
 endif()
 
-target_link_libraries(SDL2_ttf PRIVATE SDL2 Freetype)
+target_link_libraries(SDL3_ttf PRIVATE SDL3 Freetype)
 
 include(GNUInstallDirs)
-install(TARGETS SDL2_ttf
-  EXPORT sdl2_ttf-targets
+install(TARGETS SDL3_ttf
+  EXPORT sdl3_ttf-targets
   RUNTIME DESTINATION bin
   ARCHIVE DESTINATION lib
   LIBRARY DESTINATION lib)
 
-if(SDL2TTF_INSTALL)  
+if(SDL3TTF_INSTALL)
   if(NOT DEFINED SDL_TTF_SKIP_HEADERS)
-    install(FILES SDL_ttf.h DESTINATION include/SDL2)
+    install(FILES SDL_ttf.h DESTINATION include/SDL3)
   endif()
-  
+
   # CMake integration
-  configure_file(sdl2_ttf-config.cmake.in
-    "${CMAKE_CURRENT_BINARY_DIR}/${CMAKE_FILES_DIRECTORY}/sdl2_ttf-config.cmake" @ONLY)
+  configure_file(sdl3_ttf-config.cmake.in
+    "${CMAKE_CURRENT_BINARY_DIR}/${CMAKE_FILES_DIRECTORY}/sdl3_ttf-config.cmake" @ONLY)
   install(FILES
-    "${CMAKE_CURRENT_BINARY_DIR}/${CMAKE_FILES_DIRECTORY}/sdl2_ttf-config.cmake"
-    DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/SDL2_ttf/")
-  export(TARGETS SDL2_ttf
-    FILE "${CMAKE_CURRENT_BINARY_DIR}/${CMAKE_FILES_DIRECTORY}/sdl2_ttf-targets.cmake")
-  export(PACKAGE SDL2_ttf)
-  install(EXPORT sdl2_ttf-targets
-    DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/SDL2_ttf/")
+    "${CMAKE_CURRENT_BINARY_DIR}/${CMAKE_FILES_DIRECTORY}/sdl3_ttf-config.cmake"
+    DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/SDL3_ttf/")
+  export(TARGETS SDL3_ttf
+    FILE "${CMAKE_CURRENT_BINARY_DIR}/${CMAKE_FILES_DIRECTORY}/sdl3_ttf-targets.cmake")
+  export(PACKAGE SDL3_ttf)
+  install(EXPORT sdl3_ttf-targets
+    DESTINATION "${CMAKE_INSTALL_LIBDIR}/cmake/SDL3_ttf/")
 endif()
 
 message(STATUS "Link-time dependencies:")
diff --git a/SDL_ttf.c b/SDL_ttf.c
index b23ea99..a401c91 100644
--- a/SDL_ttf.c
+++ b/SDL_ttf.c
@@ -23,6 +23,14 @@
 #include <stdio.h>
 #include <stdlib.h>
 #include <string.h>
+#include <stdbool.h>
+#include <SDL3/SDL_version.h>
+
+/* Disable SDL3 old-name compatibility shims before any SDL3 header is
+   pulled in, so SDL2-era identifiers resolve to their real SDL3 names. */
+#ifndef SDL_DISABLE_OLD_NAMES
+#define SDL_DISABLE_OLD_NAMES
+#endif
 
 #include <ft2build.h>
 #include FT_FREETYPE_H
@@ -31,8 +39,8 @@
 #include FT_GLYPH_H
 #include FT_TRUETYPE_IDS_H
 
-#include "SDL.h"
-#include "SDL_endian.h"
+#include "SDL3/SDL.h"
+#include "SDL3/SDL_endian.h"
 #include "SDL_ttf.h"
 
 #ifdef HAVE_RAQM
@@ -99,7 +107,7 @@ struct _TTF_Font {
     c_glyph cache[257]; /* 257 is a prime */
 
     /* We are responsible for closing the font stream */
-    SDL_RWops *src;
+    SDL_IOStream *src;
     int freesrc;
     FT_Open_Args args;
 
@@ -286,12 +294,11 @@ static void TTF_drawLine_Blended(const TTF_Font *font, const SDL_Surface *textbu
     }
 }
 
-/* rcg06192001 get linked library's version. */
-const SDL_version *TTF_Linked_Version(void)
+/* rcg06192001 get linked library's version.
+   In SDL3 there is no SDL_version struct; use SDL_GetVersion() directly. */
+int TTF_Linked_Version(void)
 {
-    static SDL_version linked_version;
-    SDL_TTF_VERSION(&linked_version);
-    return(&linked_version);
+    return SDL_GetVersion();
 }
 
 /* This function tells the library whether UNICODE text is generally
@@ -479,17 +486,17 @@ static unsigned long RWread(
     unsigned long count
 )
 {
-    SDL_RWops *src;
+    SDL_IOStream *src;
 
-    src = (SDL_RWops *)stream->descriptor.pointer;
-    SDL_RWseek(src, (int)offset, RW_SEEK_SET);
+    src = (SDL_IOStream *)stream->descriptor.pointer;
+    SDL_SeekIO(src, (int)offset, SDL_IO_SEEK_SET);
     if (count == 0) {
         return 0;
     }
-    return (unsigned long)SDL_RWread(src, buffer, 1, (int)count);
+    return (unsigned long)SDL_ReadIO(src, buffer, (int)count);
 }
 
-TTF_Font* TTF_OpenFontIndexRW(SDL_RWops *src, int freesrc, int ptsize, long index)
+TTF_Font* TTF_OpenFontIndexRW(SDL_IOStream *src, int freesrc, int ptsize, long index)
 {
     TTF_Font* font;
     FT_Error error;
@@ -503,7 +510,7 @@ TTF_Font* TTF_OpenFontIndexRW(SDL_RWops *src, int freesrc, int ptsize, long inde
     if (!TTF_initialized) {
         TTF_SetError("Library not initialized");
         if (src && freesrc) {
-            SDL_RWclose(src);
+            SDL_CloseIO(src);
         }
         return NULL;
     }
@@ -514,11 +521,11 @@ TTF_Font* TTF_OpenFontIndexRW(SDL_RWops *src, int freesrc, int ptsize, long inde
     }
 
     /* Check to make sure we can seek in this stream */
-    position = SDL_RWtell(src);
+    position = SDL_TellIO(src);
     if (position < 0) {
         TTF_SetError("Can't seek in stream");
         if (freesrc) {
-            SDL_RWclose(src);
+            SDL_CloseIO(src);
         }
         return NULL;
     }
@@ -527,7 +534,7 @@ TTF_Font* TTF_OpenFontIndexRW(SDL_RWops *src, int freesrc, int ptsize, long inde
     if (font == NULL) {
         TTF_SetError("Out of memory");
         if (freesrc) {
-            SDL_RWclose(src);
+            SDL_CloseIO(src);
         }
         return NULL;
     }
@@ -547,7 +554,7 @@ TTF_Font* TTF_OpenFontIndexRW(SDL_RWops *src, int freesrc, int ptsize, long inde
     stream->read = RWread;
     stream->descriptor.pointer = src;
     stream->pos = (unsigned long)position;
-    stream->size = (unsigned long)(SDL_RWsize(src) - position);
+    stream->size = (unsigned long)(SDL_GetIOSize(src) - position);
 
     font->args.flags = FT_OPEN_STREAM;
     font->args.stream = stream;
@@ -675,14 +682,14 @@ TTF_Font* TTF_OpenFontIndexRW(SDL_RWops *src, int freesrc, int ptsize, long inde
     return font;
 }
 
-TTF_Font* TTF_OpenFontRW(SDL_RWops *src, int freesrc, int ptsize)
+TTF_Font* TTF_OpenFontRW(SDL_IOStream *src, int freesrc, int ptsize)
 {
     return TTF_OpenFontIndexRW(src, freesrc, ptsize, 0);
 }
 
 TTF_Font* TTF_OpenFontIndex(const char *file, int ptsize, long index)
 {
-    SDL_RWops *rw = SDL_RWFromFile(file, "rb");
+    SDL_IOStream *rw = SDL_IOFromFile(file, "rb");
     if (rw == NULL) {
         return NULL;
     }
@@ -1082,7 +1089,7 @@ void TTF_CloseFont(TTF_Font* font)
             SDL_free(font->args.stream);
         }
         if (font->freesrc) {
-            SDL_RWclose(font->src);
+            SDL_CloseIO(font->src);
         }
         SDL_free(font);
     }
@@ -1173,8 +1180,8 @@ static Uint32 UTF8_getch(const char **src, size_t *srclen)
 {
     const Uint8 *p = *(const Uint8**)src;
     size_t left = 0;
-    SDL_bool overlong = SDL_FALSE;
-    SDL_bool underflow = SDL_FALSE;
+    bool overlong = false;
+    bool underflow = false;
     Uint32 ch = UNKNOWN_UNICODE;
 
     if (*srclen == 0) {
@@ -1183,7 +1190,7 @@ static Uint32 UTF8_getch(const char **src, size_t *srclen)
     if (p[0] >= 0xFC) {
         if ((p[0] & 0xFE) == 0xFC) {
             if (p[0] == 0xFC && (p[1] & 0xFC) == 0x80) {
-                overlong = SDL_TRUE;
+                overlong = true;
             }
             ch = (Uint32) (p[0] & 0x01);
             left = 5;
@@ -1191,7 +1198,7 @@ static Uint32 UTF8_getch(const char **src, size_t *srclen)
     } else if (p[0] >= 0xF8) {
         if ((p[0] & 0xFC) == 0xF8) {
             if (p[0] == 0xF8 && (p[1] & 0xF8) == 0x80) {
-                overlong = SDL_TRUE;
+                overlong = true;
             }
             ch = (Uint32) (p[0] & 0x03);
             left = 4;
@@ -1199,7 +1206,7 @@ static Uint32 UTF8_getch(const char **src, size_t *srclen)
     } else if (p[0] >= 0xF0) {
         if ((p[0] & 0xF8) == 0xF0) {
             if (p[0] == 0xF0 && (p[1] & 0xF0) == 0x80) {
-                overlong = SDL_TRUE;
+                overlong = true;
             }
             ch = (Uint32) (p[0] & 0x07);
             left = 3;
@@ -1207,7 +1214,7 @@ static Uint32 UTF8_getch(const char **src, size_t *srclen)
     } else if (p[0] >= 0xE0) {
         if ((p[0] & 0xF0) == 0xE0) {
             if (p[0] == 0xE0 && (p[1] & 0xE0) == 0x80) {
-                overlong = SDL_TRUE;
+                overlong = true;
             }
             ch = (Uint32) (p[0] & 0x0F);
             left = 2;
@@ -1215,7 +1222,7 @@ static Uint32 UTF8_getch(const char **src, size_t *srclen)
     } else if (p[0] >= 0xC0) {
         if ((p[0] & 0xE0) == 0xC0) {
             if ((p[0] & 0xDE) == 0xC0) {
-                overlong = SDL_TRUE;
+                overlong = true;
             }
             ch = (Uint32) (p[0] & 0x1F);
             left = 1;
@@ -1240,7 +1247,7 @@ static Uint32 UTF8_getch(const char **src, size_t *srclen)
         --left;
     }
     if (left > 0) {
-        underflow = SDL_TRUE;
+        underflow = true;
     }
     /* Technically overlong sequences are invalid and should not be interpreted.
        However, it doesn't cause a security risk here and I don't see any harm in
@@ -1511,7 +1518,7 @@ SDL_Surface *TTF_RenderText_Solid(TTF_Font *font,
 SDL_Surface *TTF_RenderUTF8_Solid(TTF_Font *font,
                 const char *text, SDL_Color fg)
 {
-    SDL_bool first;
+    bool first;
     int xstart;
     int width;
     int height;
@@ -1552,7 +1559,7 @@ SDL_Surface *TTF_RenderUTF8_Solid(TTF_Font *font,
     }
 
     /* Create the target surface */
-    textbuf = SDL_CreateRGBSurface(SDL_SWSURFACE, width, height, 8, 0, 0, 0, 0);
+    textbuf = SDL_CreateSurface(width, height, SDL_PIXELFORMAT_INDEX8);
     if (textbuf == NULL) {
         raqm_destroy(rq);
         return NULL;
@@ -1563,7 +1570,7 @@ SDL_Surface *TTF_RenderUTF8_Solid(TTF_Font *font,
     dst_check = (Uint8*)textbuf->pixels + textbuf->pitch * textbuf->h;
 
     /* Fill the palette with the foreground color */
-    palette = textbuf->format->palette;
+    palette = SDL_GetSurfacePalette(textbuf);
     palette->colors[0].r = 255 - fg.r;
     palette->colors[0].g = 255 - fg.g;
     palette->colors[0].b = 255 - fg.b;
@@ -1571,17 +1578,17 @@ SDL_Surface *TTF_RenderUTF8_Solid(TTF_Font *font,
     palette->colors[1].g = fg.g;
     palette->colors[1].b = fg.b;
     palette->colors[1].a = fg.a ? fg.a : SDL_ALPHA_OPAQUE;
-    SDL_SetColorKey(textbuf, SDL_TRUE, 0);
+    SDL_SetSurfaceColorKey(textbuf, true, 0);
 
     /* Load and render each glyph */
-    first = SDL_TRUE;
+    first = true;
     xstart = 0;
     for (i = 0; i < glyph_count; i++) {
         int y_offset;
         error = Find_GlyphByIndex(font, g_info[i].index, CACHED_METRICS|CACHED_BITMAP);
         if (error) {
             TTF_SetFTError("Couldn't find glyph", error);
-            SDL_FreeSurface(textbuf);
+            SDL_DestroySurface(textbuf);
             raqm_destroy(rq);
             return NULL;
         }
@@ -1598,7 +1605,7 @@ SDL_Surface *TTF_RenderUTF8_Solid(TTF_Font *font,
         if (first && (glyph->minx < 0)) {
             xstart -= glyph->minx;
         }
-        first = SDL_FALSE;
+        first = false;
 
         for (row = 0; row < current->rows; ++row) {
             /* Make sure we don't go either over, or under the
@@ -1696,7 +1703,7 @@ SDL_Surface *TTF_RenderText_Shaded(TTF_Font *font,
 SDL_Surface *TTF_RenderUTF8_Shaded(TTF_Font *font,
                 const char *text, SDL_Color fg, SDL_Color bg)
 {
-    SDL_bool first;
+    bool first;
     int xstart;
     int width;
     int height;
@@ -1742,7 +1749,7 @@ SDL_Surface *TTF_RenderUTF8_Shaded(TTF_Font *font,
     }
 
     /* Create the target surface */
-    textbuf = SDL_CreateRGBSurface(SDL_SWSURFACE, width, height, 8, 0, 0, 0, 0);
+    textbuf = SDL_CreateSurface(width, height, SDL_PIXELFORMAT_INDEX8);
     if (textbuf == NULL) {
         raqm_destroy(rq);
         return NULL;
@@ -1764,7 +1771,7 @@ SDL_Surface *TTF_RenderUTF8_Shaded(TTF_Font *font,
     }
 
     /* Fill the palette with NUM_GRAYS levels of shading from bg to fg */
-    palette = textbuf->format->palette;
+    palette = SDL_GetSurfacePalette(textbuf);
     rdiff = fg.r - bg.r;
     gdiff = fg.g - bg.g;
     bdiff = fg.b - bg.b;
@@ -1778,14 +1785,14 @@ SDL_Surface *TTF_RenderUTF8_Shaded(TTF_Font *font,
     }
 
     /* Load and render each glyph */
-    first = SDL_TRUE;
+    first = true;
     xstart = 0;
     for (i = 0; i < glyph_count; i++) {
         int y_offset;
         error = Find_GlyphByIndex(font, g_info[i].index, CACHED_METRICS|CACHED_PIXMAP);
         if (error) {
             TTF_SetFTError("Couldn't find glyph", error);
-            SDL_FreeSurface(textbuf);
+            SDL_DestroySurface(textbuf);
             raqm_destroy(rq);
             return NULL;
         }
@@ -1801,7 +1808,7 @@ SDL_Surface *TTF_RenderUTF8_Shaded(TTF_Font *font,
         if (first && (glyph->minx < 0)) {
             xstart -= glyph->minx;
         }
-        first = SDL_FALSE;
+        first = false;
 
         current = &glyph->pixmap;
         for (row = 0; row < current->rows; ++row) {
@@ -1901,7 +1908,7 @@ SDL_Surface *TTF_RenderText_Blended(TTF_Font *font,
 SDL_Surface *TTF_RenderUTF8_Blended(TTF_Font *font,
                 const char *text, SDL_Color fg)
 {
-    SDL_bool first;
+    bool first;
     int i;
     int xstart;
     int width, height;
@@ -1941,8 +1948,7 @@ SDL_Surface *TTF_RenderUTF8_Blended(TTF_Font *font,
     }
 
     /* Create the target surface */
-    textbuf = SDL_CreateRGBSurface(SDL_SWSURFACE, width, height, 32,
-                               0x00FF0000, 0x0000FF00, 0x000000FF, 0xFF000000);
+    textbuf = SDL_CreateSurface(width, height, SDL_PIXELFORMAT_RGBA32);
     if (textbuf == NULL) {
         raqm_destroy(rq);
         TTF_SetError("SDL_Surface creation failed");
@@ -1969,17 +1975,17 @@ SDL_Surface *TTF_RenderUTF8_Blended(TTF_Font *font,
     }
 
     /* Load and render each glyph */
-    first = SDL_TRUE;
+    first = true;
     xstart = 0;
     pixel = (fg.r<<16)|(fg.g<<8)|fg.b;
-    SDL_FillRect(textbuf, NULL, pixel); /* Initialize with fg and 0 alpha */
+    SDL_FillSurfaceRect(textbuf, NULL, pixel); /* Initialize with fg and 0 alpha */
 
     for (i = 0; i < glyph_count; i++) {
         int y_offset;
         error = Find_GlyphByIndex(font, g_info[i].index, CACHED_METRICS|CACHED_PIXMAP);
         if (error) {
             TTF_SetFTError("Couldn't find glyph", error);
-            SDL_FreeSurface(textbuf);
+            SDL_DestroySurface(textbuf);
             raqm_destroy(rq);
             return NULL;
         }
@@ -2077,15 +2083,15 @@ SDL_Surface *TTF_RenderText_Blended_Wrapped(TTF_Font *font, const char *text, SD
     return surface;
 }
 
-static SDL_bool CharacterIsDelimiter(char c, const char *delimiters)
+static bool CharacterIsDelimiter(char c, const char *delimiters)
 {
     while (*delimiters) {
         if (c == *delimiters) {
-            return SDL_TRUE;
+            return true;
         }
         ++delimiters;
     }
-    return SDL_FALSE;
+    return false;
 }
 
 /* Don't define this until we have a release where we can change font rendering
@@ -2094,7 +2100,7 @@ static SDL_bool CharacterIsDelimiter(char c, const char *delimiters)
 SDL_Surface *TTF_RenderUTF8_Blended_Wrapped(TTF_Font *font,
                                     const char *text, SDL_Color fg, Uint32 wrapLength)
 {
-    SDL_bool first;
+    bool first;
     int i;
     int xstart;
     int width, height;
@@ -2223,14 +2229,14 @@ SDL_Surface *TTF_RenderUTF8_Blended_Wrapped(TTF_Font *font,
     }
 
     /* Create the target surface */
-    textbuf = SDL_CreateRGBSurface(SDL_SWSURFACE,
+    textbuf = SDL_CreateSurface(
             (numLines > 1) ? wrapLength : width,
 #ifdef TTF_USE_LINESKIP
             numLines * TTF_FontLineSkip(font),
 #else
             height * numLines + (lineSpace * (numLines - 1)),
 #endif
-            32, 0x00FF0000, 0x0000FF00, 0x000000FF, 0xFF000000);
+            SDL_PIXELFORMAT_RGBA32);
     if (textbuf == NULL) {
         if (strLines) {
             SDL_free(strLines);
@@ -2267,20 +2273,20 @@ SDL_Surface *TTF_RenderUTF8_Blended_Wrapped(TTF_Font *font,
 
     /* Load and render each glyph */
     pixel = (fg.r<<16)|(fg.g<<8)|fg.b;
-    SDL_FillRect(textbuf, NULL, pixel); /* Initialize with fg and 0 alpha */
+    SDL_FillSurfaceRect(textbuf, NULL, pixel); /* Initialize with fg and 0 alpha */
 
     for (line = 0; line < numLines; line++) {
         if (strLines) {
             text = strLines[line];
         }
-        first = SDL_TRUE;
+        first = true;
         xstart = 0;
         for (i = 0; i < glyph_count; i++) {
             int y_offset;
             error = Find_GlyphByIndex(font, g_info[i].index, CACHED_METRICS|CACHED_PIXMAP);
             if (error) {
                 TTF_SetFTError("Couldn't find glyph", error);
-                SDL_FreeSurface(textbuf);
+                SDL_DestroySurface(textbuf);
                 if (strLines) {
                     SDL_free(strLines);
                     SDL_stack_free(str);
@@ -2299,7 +2305,7 @@ SDL_Surface *TTF_RenderUTF8_Blended_Wrapped(TTF_Font *font,
             if (first && (glyph->minx < 0)) {
                 xstart -= glyph->minx;
             }
-            first = SDL_FALSE;
+            first = false;
 
             for (row = 0; row < glyph->pixmap.rows; ++row) {
                 /* Make sure we don't go either over, or under the
diff --git a/SDL_ttf.h b/SDL_ttf.h
index 5822784..f83a2d5 100644
--- a/SDL_ttf.h
+++ b/SDL_ttf.h
@@ -31,8 +31,21 @@
 #ifndef SDL_TTF_H_
 #define SDL_TTF_H_
 
-#include "SDL.h"
-#include "begin_code.h"
+/* Disable SDL3 old-name compatibility shims so SDL2-era identifiers
+   (SDL_bool, SDL_TRUE, SDL_RWops aliases, etc.) resolve to their real
+   SDL3 names instead of the *_renamed_* deprecation markers. Must be
+   defined before any SDL3 header is included. */
+#ifndef SDL_DISABLE_OLD_NAMES
+#define SDL_DISABLE_OLD_NAMES
+#endif
+
+#include "SDL3/SDL.h"
+
+/* SDL_ttf uses the old DECLSPEC/SDLCALL macros; map them to SDL3 names. */
+#ifndef DECLSPEC
+#define DECLSPEC SDL_DECLSPEC
+#endif
+
 
 /* Set up for C function definitions, even when using C++ */
 #ifdef __cplusplus
@@ -82,7 +95,7 @@ extern "C" {
    it should NOT be used to fill a version structure, instead you should
    use the SDL_TTF_VERSION() macro.
  */
-extern DECLSPEC const SDL_version * SDLCALL TTF_Linked_Version(void);
+extern DECLSPEC int SDLCALL TTF_Linked_Version(void);
 
 /* ZERO WIDTH NO-BREAKSPACE (Unicode byte order mark) */
 #define UNICODE_BOM_NATIVE  0xFEFF
@@ -106,8 +119,8 @@ extern DECLSPEC int SDLCALL TTF_Init(void);
  * is too high, the last indexed size will be the default. */
 extern DECLSPEC TTF_Font * SDLCALL TTF_OpenFont(const char *file, int ptsize);
 extern DECLSPEC TTF_Font * SDLCALL TTF_OpenFontIndex(const char *file, int ptsize, long index);
-extern DECLSPEC TTF_Font * SDLCALL TTF_OpenFontRW(SDL_RWops *src, int freesrc, int ptsize);
-extern DECLSPEC TTF_Font * SDLCALL TTF_OpenFontIndexRW(SDL_RWops *src, int freesrc, int ptsize, long index);
+extern DECLSPEC TTF_Font * SDLCALL TTF_OpenFontRW(SDL_IOStream *src, int freesrc, int ptsize);
+extern DECLSPEC TTF_Font * SDLCALL TTF_OpenFontIndexRW(SDL_IOStream *src, int freesrc, int ptsize, long index);
 
 /* Set and retrieve the font style */
 #define TTF_STYLE_NORMAL        0x00
@@ -291,7 +304,6 @@ extern DECLSPEC int TTF_GetFontKerningSizeGlyphs(TTF_Font *font, Uint16 previous
 #ifdef __cplusplus
 }
 #endif
-#include "close_code.h"
 
 #endif /* SDL_TTF_H_ */
 
diff --git a/glfont.c b/glfont.c
index 20ccec9..d259ff6 100644
--- a/glfont.c
+++ b/glfont.c
@@ -20,15 +20,15 @@
 */
 
 /* A simple program to test the text rendering feature of the TTF library */
-
-/* quiet windows compiler warnings */
-#define _CRT_SECURE_NO_WARNINGS
+
+/* quiet windows compiler warnings */
+#define _CRT_SECURE_NO_WARNINGS
 
 #include <stdlib.h>
 #include <stdio.h>
 #include <string.h>
 
-#include "SDL.h"
+#include "SDL3/SDL.h"
 #include "SDL_ttf.h"
 
 #ifdef HAVE_OPENGL
@@ -112,7 +112,7 @@ GLuint SDL_GL_LoadTexture(SDL_Surface *surface, GLfloat *texcoord)
     texcoord[2] = (GLfloat)surface->w / w;  /* Max X */
     texcoord[3] = (GLfloat)surface->h / h;  /* Max Y */
 
-    image = SDL_CreateRGBSurface(
+    image = SDL_CreateSurface(
             SDL_SWSURFACE,
             w, h,
             32,
@@ -162,7 +162,7 @@ GLuint SDL_GL_LoadTexture(SDL_Surface *surface, GLfloat *texcoord)
              GL_RGBA,
              GL_UNSIGNED_BYTE,
              image->pixels);
-    SDL_FreeSurface(image); /* No longer needed */
+    SDL_DestroySurface(image); /* No longer needed */
 
     return texture;
 }
@@ -400,7 +400,7 @@ int main(int argc, char *argv[])
     texMaxY = texcoord[3];
 
     /* We don't need the original text surface anymore */
-    SDL_FreeSurface(text);
+    SDL_DestroySurface(text);
 
     /* Initialize the GL state */
     glViewport(0, 0, WIDTH, HEIGHT);
@@ -423,13 +423,13 @@ int main(int argc, char *argv[])
     while (!done) {
         while (SDL_PollEvent(&event)) {
             switch (event.type) {
-                case SDL_MOUSEMOTION:
+                case SDL_EVENT_MOUSE_MOTION:
                 x = event.motion.x - w/2;
                 y = event.motion.y - h/2;
                 break;
 
-                case SDL_KEYDOWN:
-                case SDL_QUIT:
+                case SDL_EVENT_KEY_DOWN:
+                case SDL_EVENT_QUIT:
                 done = 1;
                 break;
                 default:
diff --git a/showfont.c b/showfont.c
index 16f3e76..9242fde 100644
--- a/showfont.c
+++ b/showfont.c
@@ -25,7 +25,7 @@
 #include <stdio.h>
 #include <string.h>
 
-#include "SDL.h"
+#include "SDL3/SDL.h"
 #include "SDL_ttf.h"
 
 #define DEFAULT_PTSIZE  18
@@ -263,7 +263,7 @@ int main(int argc, char *argv[])
         scene.captionRect.w = text->w;
         scene.captionRect.h = text->h;
         scene.caption = SDL_CreateTextureFromSurface(renderer, text);
-        SDL_FreeSurface(text);
+        SDL_DestroySurface(text);
     }
 
     /* Render and center the message */
@@ -348,7 +348,7 @@ int main(int argc, char *argv[])
             continue;
         }
         switch (event.type) {
-            case SDL_MOUSEBUTTONDOWN:
+            case SDL_EVENT_MOUSE_BUTTON_DOWN:
                 scene.messageRect.x = event.button.x - text->w/2;
                 scene.messageRect.y = event.button.y - text->h/2;
                 scene.messageRect.w = text->w;
@@ -356,15 +356,15 @@ int main(int argc, char *argv[])
                 draw_scene(renderer, &scene);
                 break;
 
-            case SDL_KEYDOWN:
-            case SDL_QUIT:
+            case SDL_EVENT_KEY_DOWN:
+            case SDL_EVENT_QUIT:
                 done = 1;
                 break;
             default:
                 break;
         }
     }
-    SDL_FreeSurface(text);
+    SDL_DestroySurface(text);
     TTF_CloseFont(font);
     SDL_DestroyTexture(scene.caption);
     SDL_DestroyTexture(scene.message);
